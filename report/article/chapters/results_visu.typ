== Tools to debug and assess the quality of the reconstruction

To help us debug and assess the quality of our neural network, we developed several Python scripts using the PyVista library @pyvista:

=== `visuFemur.py`
This was the first script we implemented. It allows us to visualize a femur mesh from a .OBJ file.
We initially used it to understand our data, and later to debug and visualize femur meshes generated by the neural network.

#figure(
  image("/resources/img/bone_visu.png", width: 40%),
  caption: [Visualization of a femur mesh using `visuFemur.py`]
)

=== `compare_femur.py`
We also implemented a script to compare two femur meshes, displaying a heat map of the differences (using the $norm(...)_1$ norm).
We mainly used it to compare original and reconstructed femurs, to debug our neural network and assess its reconstruction quality.

#figure(
  image("/resources/img/heat_map.png", width: 40%),
  caption: [Heat map of differences between two femur meshes using `compare_femur.py`]
)

=== `latent_explorer.py`
Finally, we developed a script to explore the latent space of the neural network.
It allows us to modify the 10 latent variables using sliders and see the corresponding reconstructed femur mesh in real time.

This tool is very useful for debugging and testing the neural network, especially for understanding how the latent space is structured and how the different latent variables affect the reconstructed femur shape.
It can also be used to generate new femur shapes by exploring the latent space.

#figure(
  image("/resources/img/10sliders.jpeg", width: 40%),
  caption: [Latent space explorer using `latent_explorer.py`]
)

With these sliders, we can generate a wide variety of femur shapes by moving the 10 latent variables. This was very useful for testing the generative capabilities of our neural network and for checking if it could generate realistic femur shapes.

However, by moving the sliders, we also noticed that some generated femurs were not realistic.
That's why we decided to perform a PCA on the latent space, to see if we could identify directions that lead to realistic femurs (see @encoder-results).
== Encoder results <encoder-results>

The goal of the encoder is to reduce the dimensionality of the femur shapes from 54,873 to 10. 

We decide to plot the data in the latent space using only these three components. To see if there is some structure in the data.

#figure(
  image("/resources/img/latent_space_plane_comparison.pdf", width: 70%),
  caption: [Latent space representation using the first three principal components. We draw one plane approximating the data distribution.]
)

With this plot, we can see that the data is not uniformly distributed in the latent space. We decide do a PCA on the latent space to see if we can focuse on fewer dimensions.

#v(1em)
#figure(
  image("/resources/img/pca_cumulative_variance_NeuralNetwork_centered_tanh_5000.png", width: 70%),
  caption: [Cumulative variance explained by PCA on the latent space]
)
#v(1em)

With this plot, we can see that the first six principal components explain almost 90% of the variance in the latent space. This means that we can reduce the dimensionality of the latent space from 10 to 6 without losing much information.
